# 数据边界

安装与版本检查只请求公开版本文件，不发送个人凭据、公众号身份或安装回执。客户端只向 https://wx.openlx.cn 交接本次请求。
请求只携带目标公众号、当前动作和必要的内容/素材，不上传工作区、账户清单或历史上下文。凭据只放在请求认证头，不写入技能、安装备份或状态文件。
网关保留微信授权和执行所需的状态与必要操作标识；不自动向其他业务系统同步授权或发布结果。内容和素材按当前操作送达微信，平台权限和规则仍然适用。
文件下载校验 SHA256；安装拒绝路径穿越和符号链接，本地修改更新前保护并备份。
