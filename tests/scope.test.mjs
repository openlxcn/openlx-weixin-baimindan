import {test} from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';test('trigger fixtures cover task boundaries',()=>{const p=JSON.parse(fs.readFileSync('evals/positive-trigger-cases.json'));const n=JSON.parse(fs.readFileSync('evals/negative-trigger-cases.json'));assert.equal(p.length,6);assert.equal(n.length,7);assert.ok(p.every(x=>x.should_trigger));assert.ok(n.every(x=>!x.should_trigger));const s=fs.readFileSync('skills/openlx-weixin-baimindan/SKILL.md','utf8');assert.match(s,/Do not use for writing/);assert.match(s,/explicit_publication_confirmation=true/);});
