import {test} from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('scan exact public export for secrets, local paths and retired IDs',()=>{const patterns=[/gh[pousr]_[A-Za-z0-9]{30,}/,/AKID[A-Za-z0-9]{25,}/,/-----BEGIN [A-Z ]*PRIVATE KEY-----/,/wx[a-f0-9]{16}/,/\/Users\/[^\s]+/,/C:\\Users\\/,/openlx-weixin-gzhip/,/openlx-wechat-publisher/,/openlx-weixin-publisher/];for(const f of JSON.parse(fs.readFileSync('public-package-allowlist.json'))){if(f.endsWith('.png')||f==='tests/secret-scan.test.mjs')continue;const s=fs.readFileSync(f,'utf8');for(const p of patterns)assert.ok(!p.test(s),'secret/boundary '+f);}});
