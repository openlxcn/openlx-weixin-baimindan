import {test} from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
test('explicit allowlist only, no symlinks or unexpected source files',()=>{const allow=JSON.parse(fs.readFileSync('public-package-allowlist.json'));assert.equal(new Set(allow).size,allow.length);for(const f of allow){assert.ok(!f.includes('..')&&!path.isAbsolute(f));assert.ok(fs.lstatSync(f).isFile());assert.ok(!fs.lstatSync(f).isSymbolicLink());}const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>['.git','dist','__pycache__'].includes(e.name)?[]:e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name).replaceAll('\\','/')]);for(const f of walk('.'))assert.ok(allow.includes(f),f);});
