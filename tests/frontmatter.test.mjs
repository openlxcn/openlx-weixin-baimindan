import {test} from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';test('skill name, version and links',()=>{const s=fs.readFileSync('skills/openlx-weixin-baimindan/SKILL.md','utf8');assert.match(s,/^---\nname: openlx-weixin-baimindan\n/);assert.match(s,/version: "0.1.0"/); for(const file of JSON.parse(fs.readFileSync('public-package-allowlist.json')).filter(x=>x.endsWith('.md'))){const text=fs.readFileSync(file,'utf8');for(const m of text.matchAll(/\]\(([^)]+)\)/g)){if(!m[1].startsWith('http')&&!m[1].startsWith('#'))assert.ok(fs.existsSync(path.resolve(path.dirname(file),m[1])),file+' '+m[1]);}}});
